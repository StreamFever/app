{% extends 'base.html.twig' %}

{% block title %}Map{% endblock %}

{% block body %}
    <h1>Map</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ map.id }}</td>
            </tr>
            <tr>
                <th>MapName</th>
                <td>{{ map.mapName }}</td>
            </tr>
            <tr>
                <th>MapImg</th>
                <td>{{ map.mapImg }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('app_map_index') }}"><span class="iconify" data-icon="akar-icons:arrow-back-thick"></span> Retour à la liste</a>

    <a class="btn btn-light" href="{{ path('app_map_edit', {'id': map.id}) }}"><span class="iconify" data-icon="akar-icons:edit"></span> Editer</a>

    {{ include('map/_delete_form.html.twig') }}
{% endblock %}
