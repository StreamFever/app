{% extends 'base.html.twig' %}

{% block title %}Editer l'événement "{{event.eventName is defined ? event.eventName : ""}}"
{% endblock %}

{% block body %}
	<h1>Editer l'événement -
		{{event.eventName is defined ? event.eventName : ""}}</h1>

	{{ include('event/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

	<a class="btn btn-dark" href="{{ path('app_event_index') }}">Retour à la liste</a>

	{{ include('event/_delete_form.html.twig') }}
{% endblock %}
