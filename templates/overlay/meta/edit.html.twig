{% extends 'base.html.twig' %}

{% block title %}Editer la métadonnée "{{meta.MetaKey is defined ? meta.MetaKey : ""}}"
{% endblock %}

{% block body %}
	<h1>Editer la métadonnée "{{meta.MetaKey is defined ? meta.MetaKey : ""}}"</h1>

	{{ include('overlay/meta/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

	<a class="btn btn-dark" href="{{ path('app_meta_index') }}"><span class="iconify" data-icon="akar-icons:arrow-back-thick"></span> Retour à la liste</a>

	{{ include('overlay/meta/_delete_form.html.twig') }}
{% endblock %}
