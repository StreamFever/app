{% extends 'base.html.twig' %}

{% block title %}Overlay{% endblock %}

{% block body %}
    <h1>Overlay</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ overlay.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_overlay_index') }}">back to list</a>

    <a href="{{ path('app_overlay_edit', {'id': overlay.id}) }}">edit</a>

    {{ include('overlay/_delete_form.html.twig') }}
{% endblock %}
