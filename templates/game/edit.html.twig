{% extends 'base.html.twig' %}

{% block title %}Editer le match "{{event.eventName is defined ? event.eventName : ""}}"
{% endblock %}

{% block body %}
	<pre>
    {{ dump(game) }}
</pre>
	<h1>Editer l'événement -
		{{event.eventName is defined ? event.eventName : ""}}</h1>

	{{ include('game/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

	<a class="btn btn-dark" href="{{ path('app_game_index') }}">
		<span class="iconify" data-icon="akar-icons:arrow-back-thick"></span>
		Retour à la liste</a>

	{{ include('game/_delete_form.html.twig') }}
{% endblock %}
