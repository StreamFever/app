{% extends 'base.html.twig' %}

{% block title %}Maps index{% endblock %}

{% block body %}
    <h1>Maps index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>MapName</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for map in maps %}
            <tr>
                <td>{{ map.id }}</td>
                <td>{{ map.mapName }}</td>
                <td>
                    <a href="{{ path('app_maps_show', {'id': map.id}) }}">show</a>
                    <a href="{{ path('app_maps_edit', {'id': map.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_maps_new') }}">Create new</a>
{% endblock %}
